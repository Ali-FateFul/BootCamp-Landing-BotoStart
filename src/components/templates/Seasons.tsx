import { seasonsData } from "@/fixtures/seasonsData";
import CardSeasons from "../modules/CardSeasons";

export default function Seasons() {
	return (
		<div>
			<div>
				<img />
				<h2></h2>
			</div>
            {/* {seasonsData.map(item =><CardSeasons header={item.header} fields={item.fields}/>} */}
		</div>
	);
}
